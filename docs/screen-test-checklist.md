# 画面テストチェックリスト

手動での画面テスト (受け入れテスト) 用チェックリスト。
完了した項目は `[ ]` → `[x]` に変更し、NG項目は末尾の「NG記録」に記載する。

- 実施日: 2026-07-07
- 実施者: Claude Code (claude-in-chrome ブラウザ自動操作、管理者アカウントで実施) + kenjiさん
- 対象環境: 本番 (https://techno-drone-system.com)
- 対象コミット: dev + PR #58 (本番リダイレクト修正、サーバーへ手動反映済み) + PR #59 (サイドバーリンク追加、未デプロイ)

> **テストデータの後始末**: `[E2E-TEST]` 機体・飛行計画は本セッション終了時点で**未削除**。
> Phase 3 (DIPS本番検証) で同じ計画を継続利用するため意図的に残置している。
> Phase 3 完了後に本番DBから削除すること (未対応、次回セッションの持ち越し)。

> **注意**: セクション6 (DIPS連携) は検証環境のIP制限のため本番サーバー上でのみ実施可能。
>
> **今回の自動実施の制約**: 管理者アカウントのセッションが唯一の認証手段のため、ログアウト・
> 誤パスワード再現・レート制限などセッションを失う操作は避けた。学生・操縦者用の別アカウントも
> ないため、複数アカウントにまたがる権限境界テストの一部も未実施。外部からの未認証アクセス確認は
> `curl` の実行許可が得られなかったため一部見送った。該当項目は `[ ]` のまま残し、末尾の
> 「今回自動実施できなかった項目」にまとめている。

---

## 0. 事前準備

- [x] seed 実行済みで、管理者アカウントでログインできる (seed 管理者の status が ACTIVE であること)
- [ ] テスト用の受講生アカウントが1つ以上ある (入学申請フローで作成するか seed で用意) — 未実施
- [x] テスト用の機体が1機以上登録済み (登録記号あり) — `[E2E-TEST] 画面テスト機体` を今回登録
- [ ] テスト用の動画ファイルが1本以上配置済み — 未確認

## 1. 認証 (ログイン・ログアウト)

- [x] `/login` 管理者で正しいメール・パスワード → ログイン成功し管理者ホームへ遷移
- [ ] `/login` 受講生で正しいメール・パスワード → ログイン成功し受講生ホームへ遷移 — 受講生アカウント未作成のため未実施
- [x] 誤ったパスワード → エラーメッセージ表示 (メール存在有無など内部情報を漏らさない文言) — デプロイ作業中に実際に誤パスワードでログイン試行し「メールアドレスまたはパスワードが正しくありません」を確認済み
- [ ] 存在しないメールアドレス → 同上のエラーメッセージ — 未実施
- [ ] 無効化 (INACTIVE) 済みユーザー → ログイン不可 — 未実施
- [x] 未ログイン状態で `/admin` へ直接アクセス → `/login` へリダイレクト — PR #58 検証時に role-redirect 経由で確認済み (middleware.ts が保護)
- [x] 未ログイン状態で `/student` へ直接アクセス → `/login` へリダイレクト — kenjiさんの curl 実行で確認 (`location: https://techno-drone-system.com/login`)
- [x] 未ログイン状態で `/flight/plans` へ直接アクセス → `/login` へリダイレクト — kenjiさんの curl 実行で確認。`Location: /login` と相対パスだが、こちらは Server Component の `redirect()` (`lib/serverAuth.ts`) による標準的な相対リダイレクトで、ブラウザ側で正しく解決されるため問題なし
- [ ] ログアウト → セッション破棄され保護ページに戻れない — 唯一の認証済みセッションのため未実施
- [ ] ログイン試行を短時間に連続で失敗 → レート制限が発動する — 未実施 (誤ってロックアウトするリスクを避けた)

## 2. 入学申請〜初回セットアップ (招待フロー)

- [ ] 入学申請を登録 → 管理者の `/admin/applications` に申請が表示される
- [ ] 管理者が申請を承認 → 招待メールが送信される (Resend)
- [ ] 招待メールのリンクから `/setup/password` が開ける
- [ ] パスワード設定: 弱いパスワード (短い等) → バリデーションエラー
- [ ] パスワード設定: 有効なパスワード → 次の規約同意へ進む
- [ ] `/setup/agreement` 規約に同意 → セットアップ完了、ログイン可能になる
- [ ] 同意せずに他ページへ直接アクセス → セットアップ画面へ誘導される
- [ ] 不正・期限切れの招待トークン → エラー表示 (詳細を漏らさない)
- [ ] `/admin/enrollment/new` 管理者による手動登録 → 同様に招待フローが開始される

## 3. 受講生画面

### 3-1. ホーム・コース

- [ ] `/student` 自分の進捗サマリが表示される
- [ ] `/courses` 自分にアクセス権のあるコースのみ表示される
- [ ] `/courses/[courseId]` 科目・動画一覧と進捗が表示される
- [ ] アクセス権のないコースのURL直打ち → 拒否される

### 3-2. 動画視聴

- [ ] `/courses/[courseId]/videos/[videoId]` 動画が再生できる
- [ ] 視聴後に進捗率が更新される
- [ ] 視聴ログが記録される (管理者側の受講生詳細で確認)
- [ ] 不正視聴パターン (大幅なスキップ等) → 不正フラグが記録される (`/admin/fraud-flags` で確認)

### 3-3. 修了確認試験

- [ ] `/exams` 受験可能な試験の一覧が表示される
- [ ] 受験条件 (進捗等) を満たさない試験 → 開始できない
- [ ] `/exams/[examId]` 出題 → 解答 → 提出が最後まで完了できる
- [ ] `/exams/[examId]/result` 合否・得点が表示される
- [ ] 提出済み試験の再提出 → 二重採点にならない

### 3-4. 質疑応答・修了証明書

- [ ] `/qa` 質問を投稿できる
- [ ] 管理者の回答が受講生側に表示される
- [ ] `/certificate` 修了者は修了証明書PDFをダウンロードできる
- [ ] 未修了者には証明書が表示されない

## 4. 管理者画面

### 4-1. ダッシュボード・ユーザー管理

- [x] `/admin` 統計値 (受講生数等) が実データと一致する — 表示確認済み (登録0件の状態と一致)
- [x] `/admin/users` 一覧表示 — 表示確認済み。ロールフィルタは未実施
- [ ] `/admin/users/new` ユーザー作成 → 一覧に反映される — 未実施 (メール送信を伴うため実データ作成を保留)
- [ ] ユーザー無効化 → 対象ユーザーがログイン不可になる — 未実施
- [ ] メールアドレス重複で作成 → ユーザー向けエラーメッセージ — 未実施

### 4-2. 受講生管理

- [ ] `/admin/students` 受講生一覧が表示される
- [ ] `/admin/students/[id]` 進捗・視聴ログ詳細が表示される
- [ ] `/admin/students/[id]/review` 受講確認 (成立判定) が実行できる
- [ ] `/admin/students/[id]/certificate` 修了証明書の交付ができる
- [ ] `/admin/judgments` 成立判定の一覧・記録が確認できる
- [ ] 修了証明書交付台帳 (様式5) PDFが出力できる

### 4-3. コンテンツ管理

- [x] `/admin/courses` コース一覧の表示を確認済み。新規作成・編集の実操作は未実施
- [ ] `/admin/videos` 動画の登録・科目紐付けができる — 未実施
- [ ] `/admin/questions` 問題バンクの登録・編集ができる — 未実施
- [ ] `/admin/exam-results` 試験結果の一覧が確認できる — 未実施
- [ ] `/admin/qa` 受講生の質問に回答できる — 未実施
- [ ] `/admin/fraud-flags` 不正フラグの一覧・確認ができる — 未実施

## 5. 飛行管理

### 5-1. 機体管理

- [x] `/flight/aircraft` 自分の機体一覧が表示される
- [x] `/flight/aircraft/new` 機体登録 (登録記号・重量等) ができる — `[E2E-TEST] 画面テスト機体` で確認
- [ ] 必須項目未入力 → バリデーションエラー — 未実施
- [x] `/flight/aircraft/[id]` 詳細が表示される
- [ ] `/flight/aircraft/[id]/edit` 編集が反映される — 未実施
- [ ] 機体の無効化 → 飛行計画作成時の機体選択肢に出なくなる — 未実施

### 5-2. 飛行計画

- [x] `/flight/plans` 一覧表示 — 確認済み (1件のみのためページネーションは未確認)
- [x] `/flight/plans/new` 計画作成ができる (タイトル・場所・日時・時間・目的) — `[E2E-TEST] 画面テスト飛行計画` で確認
- [ ] 過去日時を指定 → 入力できない (min属性) — 未実施
- [ ] 他人の機体IDをAPI直叩きで指定 → 拒否される — 未実施 (別アカウントなし)
- [x] `/flight/plans/[id]` 詳細・リスク情報 (落下距離) が表示される — 天候・落下距離のスタブ値が正しく表示
- [x] 管理者: DRAFT → 承認ができる — 確認済み。却下は未実施
- [ ] 却下時に確認ダイアログが表示される — 未実施
- [ ] 本人: APPROVED → 完了 (COMPLETED) にできる — 未実施
- [ ] DRAFT の計画を編集 → 反映される — **NG: 編集画面 (`/flight/plans/[id]/edit`) が実装されておらず、UI上で編集する手段がない。API (PUT) は実装済み。詳細はNG記録参照**
- [ ] APPROVED の計画を編集 → ステータスが DRAFT に戻り再承認が必要になる — 上記と同じ理由で未実施
- [ ] COMPLETED の計画 → 編集できない (エラーメッセージ表示) — 同上
- [ ] DIPS通報済みの計画 → 編集できない (エラーメッセージ表示) — 同上
- [x] 管理者は全件が見える — `/flight/plans` で自分が作成したテスト計画を確認 (一般操縦者との比較は別アカウントなしのため未実施)

### 5-3. 飛行日誌

- [x] `/flight/logs` 一覧が表示される
- [x] `/flight/logs/new` 日誌作成 (飛行前点検・飛行後点検を含む) ができる — 全項目入力し作成成功
- [x] `/flight/logs/[id]` 詳細が表示される — PDFダウンロード機能があることも確認
- [x] `/admin/flight-logs` 管理者が全日誌を確認できる — 一覧に反映されることを確認

## 6. DIPS連携 (本番サーバー検証環境限定)

> 前提: `.env` に DIPS_* 設定済み、`DIPS_ENABLED=true`、検証用 申請者ID (USR0630xx) を用意。

- [x] `DIPS_ENABLED=true` であることをサーバーで確認 (`grep "^DIPS_ENABLED=" .env`)
- [x] APPROVED かつ自分の計画の詳細に「DIPS通報」ボタンが表示される — `[E2E-TEST]` 計画で確認
- [ ] DRAFT / REJECTED / COMPLETED の計画には表示されない — 未実施
- [x] DIPS未認証で通報 → DIPSログイン画面へリダイレクトされる — `stg.uafp.dips.mlit.go.jp` の認可画面へ遷移することを確認
- [x] DIPSログイン完了 → `/redirect` 経由で計画一覧に戻る (`?dips=linked`) — kenjiさんが検証環境アカウントでログインし到達を確認
- [ ] 通報ダイアログ: 飛行目的未選択 → エラー — バリデーションメッセージ「飛行目的を1つ以上選択してください」の表示は確認したが、意図した手順ではなく `form_input` ツールの制約で偶発的に確認したもの
- [ ] 通報ダイアログ: 緯度・経度の範囲外 (例: 経度200) → エラー — 未実施
- [ ] 通報ダイアログ: 速度・高度の範囲外 (0や1000) → エラー — 未実施
- [ ] 正しい入力で通報 → 成功し「DIPS通報済み (飛行計画ID: …)」表示に変わる — **NG。NG記録#3参照**
- [ ] 通報済みの計画 → 通報ボタンが表示されず、再通報できない — 通報自体が成功しないため未確認
- [ ] 通報済みの計画 → 編集がブロックされる — 同上
- [ ] アクセストークン失効後 (5分放置) の再操作 → 自動リフレッシュされ操作が成功する — 未実施
- [ ] DIPS側 (検証環境ポータル) で通報された飛行計画が確認できる — 通報が未成立のため未実施

## 7. 権限境界

- [ ] 受講生アカウントで `/admin` 系URLへ直接アクセス → 拒否される
- [ ] 他ユーザーの飛行計画IDをURL直打ち → 404 (存在を漏らさない)
- [ ] 他ユーザーの機体IDをURL直打ち → 404
- [ ] 他受講生の試験結果・証明書へのアクセス → 拒否される
- [ ] ログアウト後にブラウザの「戻る」→ 保護ページの操作ができない

## 8. 表示・共通

- [ ] スマホ幅 (375px) で主要画面 (ログイン・一覧・詳細・フォーム) の表示が崩れない
- [ ] 日時がすべて日本時間 (JST) で表示される
- [ ] エラー時にユーザー向けメッセージが表示される (スタックトレース等が露出しない)
- [ ] 存在しないURL → 404ページが表示される
- [ ] 主要画面の初回表示が実用的な速度 (目安3秒以内)

---

## NG記録

| # | セクション | 事象 | 再現手順 | 対応 (Issue番号等) |
|---|-----------|------|---------|------------------|
| 1 | 5-2 飛行計画 | 飛行計画の編集画面 (`/flight/plans/[id]/edit`) が実装されておらず、UI上で計画内容を編集する手段が存在しない。API層 (`PUT /api/flight/plans/[id]`) は編集ロジック (承認済みはDRAFTに戻す等) を含めて実装済みだが、呼び出すページがない | `/flight/plans/[id]` の詳細画面を開いても編集リンク・ボタンが一切ない (コード確認: `app/(flight)/flight/plans/[id]/page.tsx` に編集導線なし、`app/(flight)/flight/plans/[id]/edit/` ページ自体が存在しない) | 未対応。新規ページ追加が必要な規模のため、次回セッションでの対応を提案 |
| 2 | 1 認証 / 4-1 管理者 | seed で作成される管理者アカウントが LMS上の受講ステータス (`受講中`) を持ち、`/admin/users` 一覧で「試験合格にする」等の受講生向け操作が表示される | `/admin/users` を管理者アカウントで閲覧 | 対応不要 (仕様通りの可能性が高いが要確認)。優先度低 |
| 3 | 6 DIPS連携 | 「DIPSへ通報」ダイアログを送信すると、初回は必ずDIPSのOAuth認可画面へ遷移し、連携 (トークン保存) のみが行われて実際の通報 (`dipsFlightPlanId` の記録) は行われない。ログイン完了後 `/flight/plans?dips=linked` (一覧ページ) に戻ってしまい、入力したフォーム内容も破棄されるため、ユーザーは詳細ページに戻って再度フォーム入力・送信をやり直す必要がある。2回目の送信でも同様にOAuth画面を経由し、通報自体は成功しなかった (`dipsFlightPlanId` 未記録のまま) | 承認済みの `[E2E-TEST]` 計画で「DIPSへ通報」→フォーム入力→送信を2回実施。トークン保存・読み出しのコード自体 (`(userId, realm="fpl")` キー) は正しく実装されていることをコードレビューで確認済みだが、`DipsNotifyButton.tsx` の認証エラー時のハンドリングが `window.location.href` によるページ全体遷移のため、送信予定だったフォームデータとplanIdが失われ、通報が完了しない | 未対応。修正には「OAuth連携後に元の詳細ページへ戻り、ダイアログとフォーム内容を復元して自動再送信する」導線の追加が必要。次回セッションでの対応を提案 |

## 今回自動実施できなかった項目 (次回以降の確認推奨)

- **ログアウト・誤パスワード再現・レート制限**: 唯一の認証済みセッションを失うリスクがあるため自動実施を見送った。kenjiさんによる手動確認を推奨
- **受講生・操縦者ロールでの確認全般 (セクション2・3・7の大部分)**: テスト用の別アカウントがないため未実施。入学申請フローで1名作成すれば次回まとめて自動化できる
- **未ログイン状態での `/student`・`/flight/plans` への直接アクセス確認**: `curl` の実行許可が得られなかったため未実施。以下のコマンドを実行して結果を貼っていただければ確認可能:

  ```bash
  curl -s -o /dev/null -D - https://techno-drone-system.com/student | grep -i "^location\|^HTTP"
  curl -s -o /dev/null -D - https://techno-drone-system.com/flight/plans | grep -i "^location\|^HTTP"
  ```

  (`location: https://techno-drone-system.com/login` が返ればOK)
- **メール送信を伴う操作 (ユーザー新規作成・入学申請承認)**: 実際にメールが送信されてしまうため今回は保留
- **スマホ幅表示・動画視聴・修了証明書PDF等**: 未着手
